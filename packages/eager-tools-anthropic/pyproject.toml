[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "eager-tools-anthropic"
version = "0.0.1"
description = "Anthropic adapter for eager tool calling — wraps the Messages stream to emit SealEvents."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "CloudThinker", email = "hello@cloudthinker.io" }]
keywords = ["agent", "llm", "tool-calling", "streaming", "anthropic", "claude"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Typing :: Typed",
]
dependencies = ["eager-tools-core>=0.0.1"]

[project.optional-dependencies]
dev = [
    "anthropic>=0.39",
    "pytest>=8",
    "pytest-asyncio>=0.23",
    "ruff>=0.5",
    "pyrefly>=0.1",
]

[project.urls]
Homepage = "https://eager-tools.dev"
Repository = "https://github.com/eager-tools/eager-tools"
Issues = "https://github.com/eager-tools/eager-tools/discussions"

[tool.uv.sources]
eager-tools-core = { path = "../eager-tools-core", editable = true }

[tool.hatch.build.targets.wheel]
packages = ["src/eager_tools_anthropic"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM", "RUF"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
